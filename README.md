# 📋 Анкета для анализа банкротства

Два файла: `anketa.html` + `bot.py` (email-сервис)

## 🚀 Быстрый запуск

### 1. Локально (для теста)

```bash
# Установите зависимости
pip install -r requirements.txt

# Запустите сервер
python bot.py
```

Откройте `anketa.html` в браузере

### 2. На Render.com (бесплатно)

1. Зарегистрируйтесь на [render.com](https://render.com)
2. Создайте новый **Web Service**
3. Подключите GitHub репозиторий
4. Настройки:
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python bot.py`
5. Добавьте переменные окружения:
   - `MAIL_FROM` = `form.yancodekwork@mail.ru`
   - `MAIL_PASSWORD` = `3mitsella-zhivitsa4-kruchina`
   - `MAIL_TO` = `form.yancodekwork@mail.ru` (или другой email заказчика)
6. Нажмите **Deploy**

### 3. Обновите anketa.html

После деплоя на Render получите URL (например: `https://your-app.onrender.com`)

Откройте `anketa.html` и измените:
```javascript
const API_URL = 'https://your-app.onrender.com/api/submit';
```

### 4. Разместите anketa.html

Загрузите `anketa.html` на:
- GitHub Pages
- Netlify
- Vercel
- Или просто откройте локально

## ✅ Готово!

Заполните анкету → заявка придёт на email!

## 📧 Что получает заказчик

- Красивое HTML письмо с кратким резюме
- JSON файл с полными данными (вложение)
- Все заявки сохраняются в папке `applications/`

## 🔧 Настройка другого email

Чтобы отправлять на другой email, измените в переменных окружения:
```
MAIL_TO=email_заказчика@example.com
```

## 📁 Структура

- `anketa.html` - веб-анкета (12 шагов)
- `bot.py` - email-сервис
- `requirements.txt` - зависимости Python
- `applications/` - папка с заявками (создаётся автоматически)
